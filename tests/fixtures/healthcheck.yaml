version: 1
from: nginx:latest
run: apt-get update && apt-get install -y curl
healthcheck:
  cmd: "curl -f http://localhost/ || exit 1"
  interval: 30s
  timeout: 10s
  retries: 3
  startPeriod: 40s
expose:
  - 80
  - 443
